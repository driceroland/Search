# Preserve split pairs when split view is off

The split view switch controls whether Search shows paired tabs together. Turning it off shows one tab at a time but keeps each pair, and turning it on again restores the pairs. Clearing pairs when the switch changes would make a temporary display choice erase a person's tab organization.
